import React from 'react';
import {TouchableOpacity, Alert, ScrollView, View, Text} from 'react-native';


const App: React.FC<{}> = () => {
  return (
    <View>
      <ScrollView>
        <View>
            <Text>Example</Text>
        </View>
      </ScrollView>
    </View>
  );
};

export default App;
